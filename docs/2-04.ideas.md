1) add notebook ml integration
https://github.com/jacob-bd/notebooklm-mcp-cli
put the new folder on left pane when notebook is active, listing sources
add options to list sources, and make searches etc (see all instruments)
2) may be even better
add a config to install and add new mcps
so add the server side tooling (now we have only local web one)
