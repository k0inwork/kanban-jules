1) allow agents to ask from user (sned mail and wait for answer)
this tool mut be explicitely deifined and when missing info pormt agent sto ask, not guess
2) routing must be correct, most cases we dont need jules-agent, we could use local
3) as we have jules task limit, we must reuse finsihed tasks, if we at half of limit, at least
limit must be set in settings, 
so we must have in setting a dropdown
free - 3/15 (conc-total)
and paid - 15/100
4) we could aslo add workflow listing for current branch and active pull requests when appearing, as jules signal work done wvia pull requests
but also taks state is enough, but sitll we could use this info later. think about it
5) also better suppress logs of LLM chats (or at least make them showable on will, but not by default)
6) we could either ask jules or we could trigger workflows and check them in our tasks. jules has beta auto-ci feature to check workflows and try fix the bugs if found, could be useful.
but then it should be part of the promt - the usage of github workflows fo rtestign and ci integration 
7) the artifact browser shows the taks ids, not names as folders
8) it looks the review folder is needed whenanet is askoing user something - for review.
may be it is must be done better.
9) deleting task not in TODO should be cautioned with question (running taks are precious, and clickcould be erroneous)
10) big mails are too big, as always, we better open big mails in tabbed interface, like arrtifacts or files
11) we must consolidte the appaearance of tabbed view for files/arts/mails/constituion and board view, it must be one click change always
the left pane change - (list of constituions, for example, when clicked on shift-btn, list of folder,list of mails) - also one click thing
examine the idea, we generally have very simple interface
main area may be tabbed or board
left area may be mail, folders or for examples - const list
this should be easily manageable, proposal neeeded