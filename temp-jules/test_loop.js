import { parseHallucinatedToolCalls } from './client/src/lib/ai/parseTools.js';

// Since we are running outside the TS environment, let's just make sure it compiles.
console.log("TS compile succeeded.");
